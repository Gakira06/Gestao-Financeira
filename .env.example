# Exemplo de configuração para Neon PostgreSQL
# Use a URL com pgbouncer para DATABASE_URL (para conexão do app)
DATABASE_URL="postgresql://<user>:<password>@<host>/<db>?pgbouncer=true&sslmode=require"
# Use a URL sem pgbouncer para DIRECT_URL (para migrations)
DIRECT_URL="postgresql://<user>:<password>@<host>/<db>?sslmode=require"
